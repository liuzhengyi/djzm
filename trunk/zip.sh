#! /bin/sh -
zip -r zips/action action/
zip -r zips/article article/
zip -r zips/artworks artworks/
zip -r zips/doc doc/
zip -r zips/font font/
zip -r zips/include include/
zip -r zips/install install/
zip -r zips/item item/
zip -r zips/lib lib/
zip -r zips/manage manage/
zip -r zips/pic pic/
zip -r zips/scripts scripts/
zip -r zips/styles styles/
zip -r zips/uploads uploads/

